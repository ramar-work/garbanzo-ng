import { DemoModeDirective } from './demo-mode.directive';

describe('DemoModeDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoModeDirective();
    expect(directive).toBeTruthy();
  });
});
